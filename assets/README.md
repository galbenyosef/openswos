# assets/

Extracted/processed asset output. **All contents under `extracted/` are gitignored** — they are regenerable from the originals via `../tools/`.

- `extracted/pc/` — output of PC asset extraction.
- `extracted/amiga/` — output of Amiga ADF + RNC ProPack extraction.

If you need to share extracted assets, do so out-of-band (S3, archive). Never commit them.
